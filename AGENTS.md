# DTCoder Blocklands 协作规范

## 沟通与文档

- 始终使用简体中文沟通和编写项目说明。
- 新增或调整玩法时，同步更新 `README.md` 和 `docs/ICH-GAME-DESIGN.md`。
- 涉及非遗名称、历史、工序和社区实践时，优先引用 UNESCO、文化和旅游部等权威来源。
- 非遗玩法应突出知识、技能、实践和传承，不把非遗简化成静态装饰或可炒作藏品。

## 技术栈

- 保留当前 React、TypeScript、vinext、Vite 和 npm 工程结构。
- Three.js 使用 npm ESM 导入，不改为 CDN 或 importmap。
- JS/TS 优先使用具名导入和 ESM，不使用 CommonJS。
- 不使用 `class` 编写项目业务逻辑；优先使用函数、配置对象和组合。
- 不引入非必要依赖。安装依赖前先确认标准 Web API 或已有依赖能否满足需求。

## 体素世界

- 方块坐标使用整数体素单元，渲染几何与碰撞坐标必须保持一致。
- 保持 16×16 区块、圆形渲染距离、每帧最多生成或重建两个区块。
- 区块网格只生成暴露面，并合并为每区块单网格。
- 修改边界方块时，同时标记相邻区块为脏。
- 高频帧循环数据保存在 Three.js 对象或 Ref 中，避免每帧触发 React 状态更新。
- WASD 移动必须基于相机当前四元数计算，不能绑定固定世界方向。

## 音频

- BGM 和音效使用原创程序化声音或具有明确授权的资源，不提交来源不明的音乐。
- 遵守浏览器自动播放策略：只在用户主动点击或按键后创建或恢复 `AudioContext`。
- 必须提供明显的静音/启用控制。
- 页面卸载时停止定时器、振荡器并关闭音频上下文。

## React 与无障碍

- 交互使用语义化按钮、导航、进度和对话框属性。
- Hooks 不得条件调用；事件、定时器和订阅必须在 Effect 清理函数中解除。
- 可推导的数据不要额外同步成状态；高频状态变化先比较再更新。
- 新增复杂玩法界面时拆分为独立具名组件，避免继续扩大 Three.js 主组件。
- 桌面和移动端都必须能进入、操作并退出所有核心玩法。

## 质量检查

完成代码修改后依次运行：

```bash
npm run lint
npm run build
```

- 修复真实错误后再总结，不使用 `--force` 绕过检查。
- 构建成功但出现上游工具的非阻塞警告时，应在需要时说明，但不要将警告描述为失败。
- 保留用户已有修改，不执行破坏性 Git 操作。
